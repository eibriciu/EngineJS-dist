# EngineJS-dist

Build público (gerado automaticamente) do script de customização do motor de
reservas Cloudbeds da Caboclos House.

**Não edite manualmente.** Este repositório é atualizado por uma GitHub
Action a partir do fonte privado em `eibriciu/EngineJS` a cada push na
branch `main`.

URL de produção (via jsDelivr):

```
https://cdn.jsdelivr.net/gh/eibriciu/EngineJS-dist@main/booking-engine.js
```
